# AGENTS.md — Edge Current LP 共通運用ルール

このファイルは、Claude・Codexを含むすべてのAIエージェントがこのリポジトリで作業する際に従う、正式な共通ルールです。

## 1. 正本（Source of Truth）

- **GitHubが唯一の正本**である。origin の `main` ブランチに **Push済みの内容だけ**が正式版とみなされる。
- ローカルの未Commit・未Push の変更は、正式版ではない。作業を始める前に、ローカルの状態が正式版（origin/main）と一致しているか確認する。

## 2. 人とAIの役割分担

- 仕様・デザイン・公開判断は**人**が担う。
- AI（Claude、Codexなど）は、**承認済みの指示**に基づいてのみ実装・レビューを行う。
- 仕様不足・矛盾・不明点がある場合は、**変更に着手せず**、人へ報告する。推測で文言・導線・デザインを変更しない。

## 3. 作業開始時に読む順番

1. `CLAUDE.md`
2. `AGENTS.md`（このファイル）
3. `README.md`
4. `PROJECT_STATUS.md`
5. 対象ファイル（例: `index.html`）

## 4. このLPの目的

このLPの目的は、**AI導入を売ること**ではなく、**業務改善診断の価値と、改善後の未来を伝えること**である。この目的に反する文言・導線・トーンの変更は行わない。

## 5. 仕様なしで変更してはいけないもの

以下は、明確な仕様（人からの指示）なしに変更しない。

- 現在の公開導線
- GoogleフォームURL
- CTA（文言・配置）
- 新規タブ遷移（`target="_blank"`）の挙動
- 既存のブランド文言
- 画像・アセット・外部URL・フォーム設定の**削除・置換・公開**

## 6. 外部操作の制限

以下は、**人の明確な許可なしに行わない**。

- Commit
- Push
- GitHub Pages設定
- Googleフォーム設定
- その他、リポジトリ外・公開環境に影響する操作全般

## 7. 完了時の報告

作業完了時には、必ず次を報告する。

- 変更したファイル
- 変更概要
- 表示確認が必要な箇所
- 懸念事項

## 8. AI利用基準（能力レベルの目安）

- **軽量**：文書作成、検索、軽微なレビュー
- **標準**：HTML/CSS/JavaScriptの通常変更、小中規模リファクタリング
- **高性能**：大規模な画面再設計、複雑な表示不具合の解析

作業に必要な能力が不足すると判断した場合は、**変更を行わず**、人により高い能力のモデルへの切り替えを求める。

## 9. 並行作業の禁止

**複数のAI（Claude・Codexなど）が同じ変更を同時に編集しない。** 着手前に、他のAIまたは人が同一箇所を作業中でないか（未Commit・未Push の変更、進行中のセッションなど）を確認する。
